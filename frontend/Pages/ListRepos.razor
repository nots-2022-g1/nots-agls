@page "/repos"
@inject IRepositoryViewModel _repositoryViewModel

<PageTitle>View all repositories</PageTitle>


<div class="d-flex flex-row justify-content-between align-items-center">
    <h1>Repositories &#187; View all</h1>
    <a href="/repos/add">
        <button class="btn btn-small btn-success">
            Add
        </button>
    </a>
</div>

@if(_repositoryViewModel.IsEmpty)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Naam</th>
                <th>URL</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var repository in _repositoryViewModel.Repositories)
            {
                <tr>
                    <td>@repository.Naam</td>
                    <td>
                        <a href="@repository.Url" target="_blank" rel="noopener noreferrer">
                            @repository.Url
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    protected override async Task OnInitializedAsync() => await _repositoryViewModel.LoadRepositoriesAsync();
}